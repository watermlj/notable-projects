# Double Pendulum Calculator App
A .csv file is used to specify the double pendulum parameters. The app uses 4th-order Runge Kutta methods to compute the time evolution of the double pendulum
using these parameters. Frames are generated to represent the time evolution and written to a .gif file, playing back the time evolution at 20 frames per second.
