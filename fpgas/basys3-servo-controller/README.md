# Basys3 Servo Controller Project
Signals with specific duty cycles are output depending on the FPGA switch flipped, allowing for the user
to choose the rotation they want the servo arm to assume. The signal output behavior was verified in a
testbench prior to programming the FPGA.
